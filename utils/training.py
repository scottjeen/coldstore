import numpy as np
import tensorflow as tf

def TRAIN(env, model, learning_rate, loss='mse'):
    """
    Train a model given an environment and a model.

    Parameters
    ----------
        env:            Environment
        model:          Model class
        learning_rate:  Learning rate
        loss:           Loss type

    """
    return Pass
